# 📖 دليل إدارة محتوى الموقع — Sara Portfolio

مرحباً! هذا الدليل يشرح لكِ كيف تعدلين على محتوى موقعك بنفسك بدون الحاجة لأي خبرة برمجية.

كل المحتوى موجود في مجلد واحد فقط اسمه **`src/data/`** — وكل ملف فيه يتحكم في قسم معين من الموقع.

---

## 📁 هيكل ملفات المحتوى

```
src/data/
├── personalInfo.js    ← البيانات الشخصية (الاسم، الإيميل، الهاتف)
├── projects.js        ← المشاريع (الأعمال / Case Studies)
├── certificates.js    ← الشهادات والدورات
├── timeline.js        ← المسيرة المهنية
├── achievements.js    ← الإنجازات والنسب المئوية
└── photography.js     ← معرض الصور الفوتوغرافية
```

---

## 1️⃣ تعديل البيانات الشخصية

**الملف:** `src/data/personalInfo.js`

```js
export const personalInfo = {
  nameAr: "سارة الشيخه",          // الاسم بالعربي
  nameEn: "Sara Alshikha",        // الاسم بالإنجليزي
  titleAr: "مديرة تنفيذية وإعلامية",  // المسمى الوظيفي بالعربي
  titleEn: "Executive & Media Manager", // المسمى الوظيفي بالإنجليزي
  email: "Alshika.sara@gmail.com",      // الإيميل
  phone: "+966 56 981 3616",            // رقم الهاتف
  linkedin: "https://linkedin.com/in/SaraAlshikha", // رابط لينكد إن
  location: "Saudi Arabia",             // الموقع
  yearsOfExperience: "19 Years (2004–2025)", // سنوات الخبرة
  totalProjects: "16+",                 // عدد المشاريع
  award: "EPA Gold 2025"                // الجائزة
};
```

**كيف تعدلين؟** فقط غيري النص بين علامات الاقتباس `"..."` واحفظي الملف.

---

## 2️⃣ إضافة مشروع جديد (عمل / Case Study)

**الملف:** `src/data/projects.js`

لإضافة مشروع جديد، انسخي هذا القالب وألصقيه في نهاية القائمة (قبل القوس الأخير `]`):

```js
  {
    id: 17,                            // ← رقم فريد (اجعليه الرقم التالي)
    slug: "my-new-project",            // ← اسم مختصر للرابط (بالإنجليزي، بدون مسافات، استخدمي - بدل المسافة)
    title: "Project English Title",    // ← عنوان المشروع بالإنجليزي
    titleAr: "عنوان المشروع بالعربي",   // ← عنوان المشروع بالعربي
    thumbnail: "رابط_صورة_الغلاف",     // ← رابط صورة الغلاف (من Cloudinary أو أي رابط صورة)
    axis: "Media",                     // ← التصنيف بالإنجليزي: "Media" أو "Management" أو "Digital"
    axisAr: "الإعلام",                 // ← التصنيف بالعربي: "الإعلام" أو "الإدارة" أو "الرقمي"
    organization: "Company Name",      // ← اسم الجهة بالإنجليزي
    organizationAr: "اسم الجهة",       // ← اسم الجهة بالعربي
    period: "2025",                    // ← الفترة الزمنية
    overview: "English overview...",    // ← نظرة عامة بالإنجليزي
    overviewAr: "نظرة عامة بالعربي...", // ← نظرة عامة بالعربي
    challenge: "English challenge...",  // ← التحديات بالإنجليزي
    challengeAr: "التحديات بالعربي...", // ← التحديات بالعربي
    role: "English role...",           // ← الدور بالإنجليزي
    roleAr: "الدور بالعربي...",        // ← الدور بالعربي
    solution: "English solution...",    // ← الحلول بالإنجليزي
    solutionAr: "الحلول بالعربي...",    // ← الحلول بالعربي
    outcome: "English outcome...",     // ← المخرجات بالإنجليزي
    outcomeAr: "المخرجات بالعربي...",   // ← المخرجات بالعربي
    gallery: [1],                      // ← اتركيها كما هي
    pdfRef: "اسم الملف",               // ← اسم الـ PDF (اختياري)
    pdfUrl: "رابط_الـPDF"              // ← رابط Google Drive للـ PDF (اختياري)
  },
```

> ⚠️ **مهم جداً:**
> - لا تنسي الفاصلة `,` بعد القوس `}` إلا إذا كان آخر مشروع
> - كل نص يجب أن يكون بين علامتي اقتباس `"..."`
> - الـ `id` يجب أن يكون رقماً فريداً لم يُستخدم من قبل

---

## 3️⃣ تعديل مشروع موجود

ابحثي عن المشروع في ملف `src/data/projects.js` باستخدام اسمه (بالبحث في أي محرر نصوص: Ctrl+F) ثم عدلي النص المطلوب فقط.

**مثال:** لتغيير عنوان مشروع:
```js
// قبل التعديل
title: "Old Project Title",

// بعد التعديل  
title: "New Project Title",
```

---

## 4️⃣ إضافة شهادة جديدة

**الملف:** `src/data/certificates.js`

هناك قسمان:
- **`featured`**: الشهادات الرئيسية (تظهر بصورة)
- **`additional`**: شهادات إضافية (تظهر كقائمة بدون صورة)

### لإضافة شهادة رئيسية (بصورة):
أضيفي هذا السطر داخل قائمة `featured`:
```js
{ id: 18, title: "Certificate Name", titleAr: "اسم الشهادة", institution: "Institution", institutionAr: "اسم المؤسسة", year: "2026", url: "رابط_الشهادة_PDF", image: "رابط_صورة_الشهادة" },
```

### لإضافة شهادة إضافية (بدون صورة):
أضيفي هذا السطر داخل قائمة `additional`:
```js
{ id: 18, title: "Certificate Name", titleAr: "اسم الشهادة", institution: "Institution", institutionAr: "اسم المؤسسة", year: "2026" },
```

---

## 5️⃣ إضافة محطة مهنية جديدة (Timeline)

**الملف:** `src/data/timeline.js`

```js
  {
    id: 10,                                    // ← رقم فريد تالي
    period: "2025–Present",                    // ← الفترة الزمنية
    title: "Job Title",                        // ← المسمى بالإنجليزي
    titleAr: "المسمى الوظيفي",                  // ← المسمى بالعربي
    organization: "Company Name",              // ← اسم الجهة بالإنجليزي
    organizationAr: "اسم الجهة",               // ← اسم الجهة بالعربي
    keyAchievement: "Key achievement",         // ← الإنجاز بالإنجليزي
    keyAchievementAr: "الإنجاز الرئيسي"        // ← الإنجاز بالعربي
  },
```

---

## 6️⃣ إضافة صورة فوتوغرافية جديدة

**الملف:** `src/data/photography.js`

أضيفي سطراً جديداً:
```js
  { id: 25, url: 'رابط_الصورة_من_Cloudinary', title: '' },
```

---

## 🖼️ كيف أرفع صورة جديدة؟

الصور مستضافة على **Cloudinary**. لرفع صورة جديدة:

1. ادخلي على حساب Cloudinary الخاص بالمشروع
2. ارفعي الصورة (Upload)
3. انسخي الرابط (URL) الذي يبدأ بـ `https://res.cloudinary.com/...`
4. استخدمي هذا الرابط في الملف المطلوب

---

## 🚀 كيف أنشر التعديلات على الموقع؟

الموقع مستضاف على **Vercel** ومرتبط بـ **GitHub**. لنشر أي تعديل:

### الطريقة السهلة (من GitHub مباشرة):
1. ادخلي على مستودع المشروع في GitHub
2. انتقلي للملف الذي تريدين تعديله (مثلاً `src/data/projects.js`)
3. اضغطي على أيقونة القلم ✏️ (Edit)
4. عدلي المحتوى
5. اضغطي **Commit changes**
6. Vercel سينشر التعديلات تلقائياً خلال دقيقة!

### من الكمبيوتر:
1. عدلي الملف واحفظيه
2. افتحي Terminal في مجلد المشروع
3. اكتبي الأوامر التالية:
```bash
git add .
git commit -m "تحديث المحتوى"
git push
```
4. Vercel سينشر التعديلات تلقائياً!

---

## ⚠️ قواعد مهمة عند التعديل

| القاعدة | التفاصيل |
|---------|----------|
| **لا تحذفي الفواصل** | كل سطر ينتهي بفاصلة `,` — لا تحذفيها |
| **حافظي على علامات الاقتباس** | كل نص يجب أن يكون بين `"..."` أو `'...'` |
| **لا تعدلي على ملفات خارج `src/data/`** | التصميم والبرمجة في ملفات أخرى — لا تلمسيها |
| **الـ `id` فريد دائماً** | كل عنصر يجب أن يكون له `id` مختلف عن الباقي |
| **الـ `slug` بدون مسافات** | استخدمي `-` بدل المسافات في الـ slug |
| **اختبري محلياً أولاً** | شغلي `npm run dev` وتأكدي قبل النشر |

---

## 🆘 في حالة حدوث خطأ

إذا ظهر خطأ بعد التعديل:
1. تأكدي أنكِ لم تحذفي فاصلة `,` أو علامة اقتباس `"`
2. تأكدي أن الأقواس `{}` و `[]` مغلقة بشكل صحيح
3. إذا لم تستطيعي حل المشكلة، ارجعي للنسخة السابقة من GitHub (Revert)

---

**تم إعداد هذا الدليل بواسطة فهمي  — يونيو 2026**
